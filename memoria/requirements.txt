# Memoria 内核依赖
# pip install memoria              → 核心
# pip install "memoria[server]"    → 核心 + HTTP 服务

aiosqlite>=0.19.0       # 异步 SQLite 驱动
cachetools>=7.1.0       # TTL 缓存（画像引擎）
jieba>=0.42.1           # 中文分词（检索用）

# HTTP 服务（可选）
fastapi>=0.110.0        # RESTful API 框架 [server]
uvicorn>=0.29.0         # ASGI 服务器 [server]
