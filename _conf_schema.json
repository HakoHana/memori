{
  "bot_name": {
    "type": "string",
    "description": "Bot 显示名",
    "default": "Hana",
    "hint": "在对话和记忆中使用的名称"
  },
  "recall_count": {
    "type": "int",
    "description": "每次召回记忆条数",
    "default": 5,
    "hint": "每次消息处理时最多召回多少条记忆"
  },
  "trigger_msg_count": {
    "type": "int",
    "description": "多少条消息后自动整理记忆",
    "default": 10,
    "hint": "达到此消息数后触发 LLM 整理记忆"
  },
  "trigger_time_minutes": {
    "type": "int",
    "description": "多少分钟无消息后自动整理",
    "default": 360,
    "hint": "距离上次整理超过此时间则触发"
  },
  "api_host": {
    "type": "string",
    "description": "Dashboard 监听地址",
    "default": "127.0.0.1",
    "hint": "设为 0.0.0.0 允许外部访问"
  },
  "api_port": {
    "type": "int",
    "description": "Dashboard 端口",
    "default": 8765,
    "hint": "Dashboard 和 REST API 端口"
  },
  "recall_context_messages": {
    "type": "int",
    "description": "召回上下文消息数",
    "default": 5,
    "hint": "召回记忆时取最近 N 条消息作为查询上下文（0=仅用当前消息）"
  },
  "embed_provider_id": {
    "type": "string",
    "description": "嵌入向量提供商 ID",
    "default": "",
    "hint": "设为 ollama-bge 使用本地 ollama bge-m3"
  },
  "embed_ollama_api_base": {
    "type": "string",
    "description": "Ollama API 地址",
    "default": "http://localhost:11434",
    "hint": "embed_provider_id 为 ollama-bge 时使用"
  },
  "embed_ollama_model": {
    "type": "string",
    "description": "Ollama 嵌入模型名",
    "default": "bge-m3",
    "hint": "embed_provider_id 为 ollama-bge 时使用"
  }
}
